language: php

sudo: false

php:
  - 7.0
  - 7.1
  - 7.2
  - nightly

before_script:
  - composer install --no-interaction --prefer-source --dev

script: make test

matrix:
  include: 
    - php: hhvm
      dist: trusty
  fast_finish: true
